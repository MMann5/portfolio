# Deferred Work

## Deferred from: code review of story-1.2a (2026-05-12)

- ~~**Italique Cormorant non chargé**~~ — **RÉSOLU (Story 2.1, 2026-05-12)** : `Cormorant_Garamond` est désormais instancié avec `style: ["normal", "italic"]` dans `src/app/[locale]/layout.tsx` — les wordmarks du marquee clients utilisent la vraie face italique (plus de faux-italique synthétique). Le commentaire « pas de version variable sur Google Fonts » a été conservé (exact pour cette famille) et reformulé pour mentionner aussi le choix explicite des styles.
- **`_global-error` hors arbre du root layout** — une page `app/global-error.tsx` (si ajoutée) rend son propre `<html>`/`<body>` et ne reçoit donc pas les classes `*.variable` de `next/font` ; les `var(--font-*)` y retomberaient sur les fallbacks (`-apple-system`, `ui-monospace`, `Georgia`). À garder en tête si une page d'erreur globale stylée est introduite (Epic 4).

## Deferred from: code review of story-2.1 (2026-05-12)

- **Lien LinkedIn — « ouvre un nouvel onglet » non annoncé à l'AT** — le CTA LinkedIn du hero (`target="_blank" rel="noopener noreferrer"`) ne signale l'ouverture en nouvel onglet que via le glyphe `↗`, qui est `aria-hidden`. Pas de texte visually-hidden ni d'`aria-label` complémentaire (cf. WCAG G201). Choix délibéré documenté dans la Story 2.1 (« le libellé visible LinkedIn suffit comme nom accessible ») ; à arbitrer lors de l'audit a11y exhaustif — **Story 4.1**. (S'applique aussi, le cas échéant, aux autres liens sortants ajoutés en Epic 2.)

## Deferred from: code review of story-1.2b (2026-05-12)

- **`config.matcher` du proxy — exclusion de métadonnées non exhaustive** — `src/proxy.ts` exclut `_next/static`, `_next/image`, `favicon.ico`, `sitemap.xml`, `robots.txt` et tout chemin avec un `.` (`.*\..*`). Les routes de métadonnées App Router **sans extension** (`/opengraph-image`, `/twitter-image`, `/icon`, `/apple-icon`) et toute future route `/api/*` ne sont pas exclues : le proxy les intercepterait et émettrait un 307 vers `/<locale>/...`. Aucune de ces routes n'existe encore. À traiter quand la Story 4.3 (SEO — OG/Twitter images, sitemap, robots) ou une route API sera ajoutée : étendre le `matcher` (ou ajouter un garde `|api/|opengraph-image|...`).

## Deferred from: code review of story-1.3 (2026-05-12)

- **Garde de complétude FR/EN aveugle aux tableaux** — `en.ts` n'utilisant pas `as const` (exigé par le spec pour permettre `fr satisfies Dictionary`), `typeof en` élargit tous les tableaux (`hero.meta`, `experience.roles[].bullets/tags/kpis`, `clients.items`, `stack.groups[].items`, `contact.secondaryLinks`…). Un traducteur qui retire un `bullet`, un `kpi`, un `tag` ou l'un des 4 `clients.items` dans `fr.ts` ne casse **pas** `npm run typecheck` — l'affirmation d'AC#1 « une clé manquante casse typecheck » ne tient que pour les clés d'objet, pas le contenu des tableaux qui constitue l'essentiel du nouveau modèle. Revisiter si une garantie de parité plus forte est voulue : tuples à longueur fixe (`as const` ciblé + helper de widening) ou test runtime comparant la forme de `fr` et `en`.
- **`scroll-mt-24` = nombre magique non dérivé de la hauteur de la `Nav`** — `GridSection` applique `scroll-mt-24` (96px) en compensation de la `Nav` sticky, mais la nav fait ~52–68px (et bien plus quand le menu mobile est ouvert / au zoom 200%). Les deep-links (`#about`…) atterrissent à une position imprécise. Introduire un token `--nav-height` (mesuré ou fixé) et baser `scroll-margin-top` dessus. → Epic 2 / Story 4.x.
- **Menu mobile : aucune gestion du focus, pas de fermeture par Échap** — ouvrir le menu ne déplace pas le focus dans le panneau ; fermer via un lien de section (`onClick={() => setMenuOpen(false)}`) démonte le panneau et fait tomber le focus sur `<body>` ; pas de handler `keydown` pour Échap. L'audit clavier/ARIA exhaustif est explicitement cadré pour la **Story 4.1** (note de portée de la Story 1.3).
- **`GridSection padded={false}` ne dégage pas le contenu des rails latéraux** — avec `padded={false}` le `<div>` interne est simplement `relative` ; les rails décoratifs sont `absolute … w-gutter`. Le seul consommateur actuel (`clients`) réajoute `px-section-x*` (plus large que `w-gutter`) et passe donc à côté, mais un futur consommateur de la variante non-paddée pourrait rendre son contenu sous les rails. Envisager un plancher `sm:px-gutter` même en `padded={false}`, ou documenter la contrainte. (Comportement par design selon le JSDoc du prop — à durcir seulement.)
- **`MMLogo` : centrage du texte fragile** — la marque « MM » centre son `<text>` via `dominantBaseline="middle"` + un offset `y="56%"` (le `56%` trahit un ajustement manuel). `dominantBaseline` a un support historiquement instable (vieux Safari). Vérifier en QA visuelle multi-navigateurs ; si besoin, passer à une marque tracée en `<path>` (pas de dépendance à la métrique de police).
- **Tags d'`experience` : source `content.js` au lieu de `content.md`** — `content.md` (désigné comme l'autorité par AC#1) donne aux deux rôles `React · TypeScript · Tailwind CSS · Supabase · Team leadership` ; le dictionnaire livre les valeurs (plus spécifiques) de `content.js`. Déviation littérale mais défendable. À confirmer lors de la finition de contenu FR/EN — **Story 2.4**.
- **`projectMeta: []` s'élargit en `never[]`** — sur le projet « AI-Driven Development Methodology », `projectMeta: []` dans `en.ts` est inféré `never[]` par `typeof en` ; lui ajouter plus tard un `projectMeta` non vide (dans `en.ts` ou `fr.ts`) casserait `typecheck` pour une raison non évidente. Si ce projet doit un jour avoir des métadonnées, prévoir un type explicite pour le champ.

## Deferred from: code review of story-2.2 (2026-05-12)

- **Clés React dérivées du contenu dans les composants de la story 2.2** — `Experience`/`FreelanceEngagements` utilisent `key={role.company}` / `key={m.name}` ; `RoleCard`/`MissionCard` utilisent `key={kpi.label}` et `key={t}` (tags). Aucun défaut actuel (valeurs uniques dans `en.ts`/`fr.ts`, listes statiques), mais rien ne garantit l'unicité à l'avenir — deux entreprises/missions/labels-KPI/tags identiques dans une même carte déclencheraient un warning React et un risque de mauvaise réconciliation. Envisager des clés par index (cohérent avec les bullets/paragraphes) ou des id stables.

## Deferred from: code review of story-2.3 (2026-05-13)

- **Construction d'URL fragile dans `MaqomCard`** — `href={\`https://${url}\`}` ([src/components/MaqomCard.tsx:101](src/components/MaqomCard.tsx#L101)) concatène le schéma `https://` sans normaliser l'input. Si un futur item écrit `url: "https://maqom.co"` (schéma inclus), `"http://x.co"`, une URL avec espaces, ou la chaîne littérale `"undefined"`/`"null"`, le `href` devient invalide (`https://https://…`, `https://maqom co`, etc.). Risque purement théorique aujourd'hui (`url: "maqom.co"` propre, dictionnaire typé) — à normaliser pendant l'audit de contenu/liens de **Story 2.4**. Fix simple possible : `(item.url ?? "").replace(/^https?:\/\//, "").trim()`, ou utiliser `new URL(item.url, "https://")`.
- **Invariant `item.url ≠ null` dans `MaqomCard` non vérifié à l'exécution ni au type** — [src/components/MaqomCard.tsx:25-27](src/components/MaqomCard.tsx#L25-L27) documente l'invariant en commentaire mais le type des props reste `ProjectItem` (avec `url: string | null`). Aujourd'hui `Projects.tsx` garantit l'invariant via le dispatch sur `featured`. Si un futur dev importe `MaqomCard` directement avec un item `featured: false, url: null`, le lien rend `href="https://"` (URL invalide, focusable, sans nom accessible). Affermir : type plus strict (`item: ProjectItem & { url: string }`) ou guard runtime `if (!item.url) return null;`.
- **Clés React dérivées du contenu — propagation de la dette review 2.2** — `Stack`/`MaqomCard`/`MethodologyCard` continuent d'utiliser `key={group.title}`, `key={it}`, `key={s}`, `key={m.label}`. La spec 2.3 montre elle-même ces snippets (contradiction interne ; les Completion Notes l'assument car « listes statiques »). Risque actuel nul (valeurs uniques). À regrouper avec la dette équivalente de 2.2 dans un éventuel passage de polish (Story 9.1 ou 4.x).
- **`statusSnake` non robuste aux accents/apostrophes/espaces insécables FR** — [src/components/MethodologyCard.tsx:15](src/components/MethodologyCard.tsx#L15) : `item.status.toLowerCase().replace(/ /g, "_")` ne normalise ni les diacritiques (`"À l'étude"` → `"à_l'étude"`), ni les apostrophes typographiques (`'`), ni ` `. Aujourd'hui FR `"En production"` → `"en_production"` (correct). Le libellé est `aria-hidden` (déco pure) donc impact a11y nul, mais visuel bruyant si un futur statut FR introduit des caractères spéciaux. Slugifier proprement si nécessaire : `.normalize("NFKD").replace(/[̀-ͯ]/g, "").replace(/[^a-z0-9]+/g, "_")`.
- **Nom long carte méthodo (`<h3>` `text-display-sm` 28px) — risque overflow à ~320px** — [src/components/MethodologyCard.tsx:29-31](src/components/MethodologyCard.tsx#L29-L31) : « AI-Driven Development Methodology » (35 caractères) en `text-display-sm` sans `break-words` ni `min-w-0` parent peut overflower sur très petits viewports. La spec 2.3 cible 375px ; audit fin 320px = **Story 4.2** (Core Web Vitals / responsive). Conforme spec actuelle.

## Deferred from: code review of story-3.1 (2026-05-13)

- **Pas de listener `matchMedia('change')` sur `prefers-reduced-motion` dans `useScrollFadeIn`** — [src/hooks/useScrollFadeIn.ts:14-22](src/hooks/useScrollFadeIn.ts#L14-L22) lit la media query une seule fois au montage du composant. Si l'utilisateur active/désactive PRM en cours de session (OS-level), les wrappers `FadeIn` déjà montés ne se ré-évaluent pas — asymétrie vs `Clients.tsx` dont le `@media (prefers-reduced-motion: reduce)` CSS est ré-évalué dynamiquement par le navigateur. Impact faible (changement de préférence en cours de session est rare). Largement résolu si le patch `motion-reduce:transition-none` est appliqué (transition CSS suit la media query en temps réel). Sinon, ajouter `mql.addEventListener('change', handler)` dans l'effect.
- **Contrat `items: readonly { name: string }[]` n'enforce ni non-vide ni longueur minimale pour le marquee** — [src/components/Clients.tsx:9](src/components/Clients.tsx#L9). Avec `items === []`, `tripled` est vide → animation tourne sur un container 0px (CPU négligeable). Avec `items.length === 1`, `translateX(-33.333%)` ne correspond plus à un cycle visuellement seamless. Aujourd'hui le dictionnaire fournit 4 items (`en.ts:76-81`, `fr.ts`) — pas de risque réel. À durcir si la liste devient dynamique : type tuple `readonly [{ name: string }, { name: string }, ...]` ou guard `if (items.length < 2) return null;`.
- **`width: max-content` peut être plus étroit que le viewport sur tablette si `items.name` sont courts** — [src/components/Clients.tsx:31](src/components/Clients.tsx#L31). Sur iPad 1024–1180 px, 12 wordmarks longs (`text-marquee` 44px) + séparateurs ≈ 2300–3500 px → la marge est confortable avec les 4 maisons actuelles ("Louis Vuitton", "Dior", "Messika", "Tiffany & Co."). Si un futur item est court (ex. "MM"), le triplicat peut tomber sous la largeur du viewport, révélant du `bg-bg-alt` à droite pendant le cycle. Mitigation : `min-width: 200vw` sur l'inner container, ou logique de duplication ×N adaptative basée sur la longueur totale mesurée.

## Deferred from: code review of story-3.2 (2026-05-13)

- **`mix-blend-mode: difference` peut réduire le contraste du dot par-dessus la `Nav` `backdrop-filter`** — [src/app/globals.css:192](src/app/globals.css#L192) (`mix-blend-mode: difference` sur `.cursor-dot`) + [src/components/Nav.tsx:58](src/components/Nav.tsx#L58) (`backdrop-blur-md`). Chaque élément crée son propre stacking context : le dot ne mixe qu'avec les pixels composités du calque ambient, pas avec ce qui est sous la nav. Selon la couleur du résultat composité, le dot peut s'approcher du `#d4a574` (quasi-invisible) ou rester lisible. Audit visuel ciblé recommandé en **Story 9.1** (polish pré-lancement) ou Story 4.2.
- **`html.cursor-none` ne couvre pas `input/textarea/select/[contenteditable]/label/summary/[role="button"]`** — [src/app/globals.css:174-178](src/app/globals.css#L174-L178) — les UA stylesheets imposent `cursor: text` (inputs) ou `cursor: pointer` (`[role="button"]`) avec une spécificité ≥ `body`. Sur ces éléments, le curseur système réapparaît en double du custom. Aucun formulaire dans le site MVP — élargir la liste (ou passer à `html.cursor-none *`) quand **Story 4.x / 9.1** introduira inputs ou contact form.
- **`requestAnimationFrame` tourne en continu même quand la souris est immobile** — [src/components/CustomCursor.tsx:68-73](src/components/CustomCursor.tsx#L68-L73) — la boucle se réenchaîne inconditionnellement. Sur laptop sur batterie, RAF 60-120 fps avec `mix-blend-mode` coûteux draine inutilement. Micro-optim : pause RAF quand `|x-rx| + |y-ry| < 0.1`, reprise au prochain `mousemove`. Différer à **Story 4.2** (Core Web Vitals / perf budget).
- ~~**Pas de retour visuel du curseur custom sur navigation clavier (focus)**~~ — **RÉSOLU (Story 4.1 AC#7, 2026-05-13)** : décision documentée dans `CustomCursor.tsx` — l'anneau `focus-visible` accent (outline 2px) reste l'indicateur primaire de focus clavier ; doubler avec un déplacement du ring custom serait redondant et coûteux. Le curseur est délibérément souris-only (`(hover: hover) and (pointer: fine)`).
- **Curseur custom reste figé en bord de viewport quand la souris quitte la fenêtre** — pas de listener `mouseleave` sur `document`/`window` dans [src/components/CustomCursor.tsx](src/components/CustomCursor.tsx). Dot/ring stationnent au bord puis snappent au retour de la souris par un autre bord. Polish différé — non bloquant.
- **`z-index: 9999/9998` valeurs magiques sans token partagé** — [src/app/globals.css:188, 204](src/app/globals.css#L188) — aucune collision aujourd'hui (pas de modal/toast). À documenter en convention (token `--z-cursor`) quand des overlays (modal, dialog Radix, toast Story 5.x) seront introduits.
- **Iframes : `mousemove` ne propage pas au document parent** — [src/components/CustomCursor.tsx:95](src/components/CustomCursor.tsx#L95) — aucun iframe aujourd'hui. Une future intégration vidéo/embed (Vimeo, Spotify, YouTube) figerait le curseur custom au bord de l'iframe. À traiter au moment de l'intégration (embed video → cacher curseur ou afficher système au survol de l'iframe).
- **OS-rendered dropdowns / `<select>` natifs / `<input type="file">` ne sont pas couverts** — [src/components/CustomCursor.tsx](src/components/CustomCursor.tsx) — pas de `<select>` natifs ni d'upload aujourd'hui. Quand **Story 4.x / 9.1** introduira un formulaire, écouter `pointerlockchange`/`fullscreenchange` ou démonter le curseur custom au focus d'un control natif.

## Deferred from: code review of story-2.4 (2026-05-13)

- **PII (URL LinkedIn + numéro téléphone) dupliqués dans `en.ts` et `fr.ts`** — `en.ts:334–335` / `fr.ts` — `secondaryLinks[0].value` (URL LinkedIn) et `[1].value` (téléphone) sont identiques dans les deux locales (non localisées) mais stockés en double. Tout changement d'une des deux valeurs nécessite deux éditions séparées pouvant diverger silencieusement — la garde `satisfies Dictionary` est aveugle au contenu des tableaux. Consolider dans `meta.linkedin` / `meta.phone` lors de **Story 9.1** (fix LinkedIn 404 déjà prévu).
- **`tel:` normalisation trop étroite dans `Contact`** — [src/components/Contact.tsx:100](src/components/Contact.tsx#L100) — `.replace(/\s+/g, "")` ne strip que les espaces. Safe avec `+972 58 422 0567`; un format avec parenthèses (`+972 (58) 422-0567`) ou tirets produirait un `tel:` URI techniquement valide mais rejeté par certains dialers. Étendre le regex si le format du numéro change.
- **Entrées non-cliquables (Location, Languages) : affordance identique aux `<a>` cliquables** — [src/components/Contact.tsx:132–136](src/components/Contact.tsx#L132-L136) — même border/padding/background que LinkedIn et Phone. Un utilisateur au clavier voit deux entrées focusables (LinkedIn, Phone) et deux qui ne le sont pas, avec exactement le même rendu visuel. Design intentionnel (spec 2.4 l'assume) ; à adresser lors de l'audit a11y — **Story 4.1**.
- **Smoke browser à ~320px non exécuté pour la section Contact** — [src/components/Contact.tsx:66–84](src/components/Contact.tsx#L66-L84) — bouton email (`michael.mann55@gmail.com`, 27 chars) avec `flex-wrap` présent; aucun débordement attendu mais jamais vérifié en browser (dev agent déféré). Vérifier lors de l'audit responsive fin — **Story 9.1**.
