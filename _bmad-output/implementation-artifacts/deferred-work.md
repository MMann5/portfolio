# Deferred Work

## Deferred from: code review of story-1.2a (2026-05-12)

- ~~**Italique Cormorant non chargé**~~ — **RÉSOLU (Story 2.1, 2026-05-12)** : `Cormorant_Garamond` est désormais instancié avec `style: ["normal", "italic"]` dans `src/app/[locale]/layout.tsx` — les wordmarks du marquee clients utilisent la vraie face italique (plus de faux-italique synthétique). Le commentaire « pas de version variable sur Google Fonts » a été conservé (exact pour cette famille) et reformulé pour mentionner aussi le choix explicite des styles.
- **`_global-error` hors arbre du root layout** — une page `app/global-error.tsx` (si ajoutée) rend son propre `<html>`/`<body>` et ne reçoit donc pas les classes `*.variable` de `next/font` ; les `var(--font-*)` y retomberaient sur les fallbacks (`-apple-system`, `ui-monospace`, `Georgia`). À garder en tête si une page d'erreur globale stylée est introduite (Epic 4).

## Deferred from: code review of story-2.1 (2026-05-12)

- ~~**Lien LinkedIn — « ouvre un nouvel onglet » non annoncé à l'AT**~~ — **RÉSOLU (Story 4.1 AC#5, 2026-05-13)** : pattern WCAG G201 implémenté sur les 4 sites de liens externes (`Hero` LinkedIn, `Contact` LinkedIn, `MissionCard` lien sortant, `MaqomCard` lien sortant). Libellé visible préservé + suffixe `<span className="sr-only"> {opensInNewTabLabel}</span>` pour l'annonce phonétique aux AT. Libellés EN `"(opens in a new tab)"` / FR `"(ouvre un nouvel onglet)"` (clé `dict.a11y.opensInNewTab`).

## Deferred from: code review of story-1.2b (2026-05-12)

- ~~**`config.matcher` du proxy — exclusion de métadonnées non exhaustive**~~ — **RÉSOLU (Story 4.3 AC#10, 2026-05-13)** : matcher étendu pour exclure `opengraph-image`, `icon`, `apple-icon`, `manifest.webmanifest` (routes Next 16 sans extension). Vérifié via `curl -v` que ces routes retournent 200 direct sans 307 vers `/en/<route>`.

## Deferred from: code review of story-1.3 (2026-05-12)

- **Garde de complétude FR/EN aveugle aux tableaux** — `en.ts` n'utilisant pas `as const` (exigé par le spec pour permettre `fr satisfies Dictionary`), `typeof en` élargit tous les tableaux (`hero.meta`, `experience.roles[].bullets/tags/kpis`, `clients.items`, `stack.groups[].items`, `contact.secondaryLinks`…). Un traducteur qui retire un `bullet`, un `kpi`, un `tag` ou l'un des 4 `clients.items` dans `fr.ts` ne casse **pas** `npm run typecheck` — l'affirmation d'AC#1 « une clé manquante casse typecheck » ne tient que pour les clés d'objet, pas le contenu des tableaux qui constitue l'essentiel du nouveau modèle. Revisiter si une garantie de parité plus forte est voulue : tuples à longueur fixe (`as const` ciblé + helper de widening) ou test runtime comparant la forme de `fr` et `en`.
- ~~**`scroll-mt-24` = nombre magique non dérivé de la hauteur de la `Nav`**~~ — **RÉSOLU (Story 4.1 AC#3, 2026-05-13)** : token `--spacing-nav-height: 72px` introduit dans `@theme` de `globals.css` ; `GridSection.tsx` consomme `scroll-mt-nav-height` (utilitaire Tailwind v4 dérivé automatiquement du préfixe `--spacing-*`). En complément, un `ResizeObserver` dans `Nav.tsx` (Story 4.1 review patch P10) met à jour `--nav-height` dynamiquement au runtime — couvre le zoom 200%, l'ouverture du menu mobile et les changements de viewport.
- ~~**Menu mobile : aucune gestion du focus, pas de fermeture par Échap**~~ — **RÉSOLU (Story 4.1 AC#4, 2026-05-13)** : pattern ARIA APG `disclosure` implémenté dans `Nav.tsx`. À l'ouverture, focus déplacé sur le premier élément focusable du panneau via `useEffect [menuOpen]` (avec `{ preventScroll: true }` — Story 4.1 review patch P1). Listener `keydown` Escape scopé au `panelRef` (Story 4.1 review patch P4) ferme le menu et rend le focus au bouton bascule. Le `onClick` sur un lien du panneau diffère la navigation après démontage via `requestAnimationFrame` (Story 4.1 review patch P9) pour que `scroll-margin-top` soit calculé sur la bonne hauteur de nav. Pas de focus-trap cyclique (intentionnel — pattern disclosure ≠ modal).
- **`GridSection padded={false}` ne dégage pas le contenu des rails latéraux** — avec `padded={false}` le `<div>` interne est simplement `relative` ; les rails décoratifs sont `absolute … w-gutter`. Le seul consommateur actuel (`clients`) réajoute `px-section-x*` (plus large que `w-gutter`) et passe donc à côté, mais un futur consommateur de la variante non-paddée pourrait rendre son contenu sous les rails. Envisager un plancher `sm:px-gutter` même en `padded={false}`, ou documenter la contrainte. (Comportement par design selon le JSDoc du prop — à durcir seulement.)
- **`MMLogo` : centrage du texte fragile** — la marque « MM » centre son `<text>` via `dominantBaseline="middle"` + un offset `y="56%"` (le `56%` trahit un ajustement manuel). `dominantBaseline` a un support historiquement instable (vieux Safari). Vérifier en QA visuelle multi-navigateurs ; si besoin, passer à une marque tracée en `<path>` (pas de dépendance à la métrique de police).
- **Tags d'`experience` : source `content.js` au lieu de `content.md`** — `content.md` (désigné comme l'autorité par AC#1) donne aux deux rôles `React · TypeScript · Tailwind CSS · Supabase · Team leadership` ; le dictionnaire livre les valeurs (plus spécifiques) de `content.js`. Déviation littérale mais défendable. À confirmer lors de la finition de contenu FR/EN — **Story 2.4**.
- **`projectMeta: []` s'élargit en `never[]`** — sur le projet « AI-Driven Development Methodology », `projectMeta: []` dans `en.ts` est inféré `never[]` par `typeof en` ; lui ajouter plus tard un `projectMeta` non vide (dans `en.ts` ou `fr.ts`) casserait `typecheck` pour une raison non évidente. Si ce projet doit un jour avoir des métadonnées, prévoir un type explicite pour le champ.

## Deferred from: code review of story-2.2 (2026-05-12)

- **Clés React dérivées du contenu dans les composants de la story 2.2** — `Experience`/`FreelanceEngagements` utilisent `key={role.company}` / `key={m.name}` ; `RoleCard`/`MissionCard` utilisent `key={kpi.label}` et `key={t}` (tags). Aucun défaut actuel (valeurs uniques dans `en.ts`/`fr.ts`, listes statiques), mais rien ne garantit l'unicité à l'avenir — deux entreprises/missions/labels-KPI/tags identiques dans une même carte déclencheraient un warning React et un risque de mauvaise réconciliation. Envisager des clés par index (cohérent avec les bullets/paragraphes) ou des id stables.

## Deferred from: code review of story-2.3 (2026-05-13)

- **Construction d'URL fragile dans `MaqomCard`** — `href={\`https://${url}\`}` ([src/components/MaqomCard.tsx:101](src/components/MaqomCard.tsx#L101)) concatène le schéma `https://` sans normaliser l'input. Si un futur item écrit `url: "https://maqom.co"` (schéma inclus), `"http://x.co"`, une URL avec espaces, ou la chaîne littérale `"undefined"`/`"null"`, le `href` devient invalide (`https://https://…`, `https://maqom co`, etc.). Risque purement théorique aujourd'hui (`url: "maqom.co"` propre, dictionnaire typé) — à normaliser pendant l'audit de contenu/liens de **Story 2.4**. Fix simple possible : `(item.url ?? "").replace(/^https?:\/\//, "").trim()`, ou utiliser `new URL(item.url, "https://")`.
- **Invariant `item.url ≠ null` dans `MaqomCard` non vérifié à l'exécution ni au type** — [src/components/MaqomCard.tsx:25-27](src/components/MaqomCard.tsx#L25-L27) documente l'invariant en commentaire mais le type des props reste `ProjectItem` (avec `url: string | null`). Aujourd'hui `Projects.tsx` garantit l'invariant via le dispatch sur `featured`. Si un futur dev importe `MaqomCard` directement avec un item `featured: false, url: null`, le lien rend `href="https://"` (URL invalide, focusable, sans nom accessible). Affermir : type plus strict (`item: ProjectItem & { url: string }`) ou guard runtime `if (!item.url) return null;`.
- **Clés React dérivées du contenu — propagation de la dette review 2.2** — `Stack`/`MaqomCard`/`MethodologyCard` continuent d'utiliser `key={group.title}`, `key={it}`, `key={s}`, `key={m.label}`. La spec 2.3 montre elle-même ces snippets (contradiction interne ; les Completion Notes l'assument car « listes statiques »). Risque actuel nul (valeurs uniques). À regrouper avec la dette équivalente de 2.2 dans un éventuel passage de polish (Story 9.1 ou 4.x).
- **`statusSnake` non robuste aux accents/apostrophes/espaces insécables FR** — [src/components/MethodologyCard.tsx:15](src/components/MethodologyCard.tsx#L15) : `item.status.toLowerCase().replace(/ /g, "_")` ne normalise ni les diacritiques (`"À l'étude"` → `"à_l'étude"`), ni les apostrophes typographiques (`'`), ni ` `. Aujourd'hui FR `"En production"` → `"en_production"` (correct). Le libellé est `aria-hidden` (déco pure) donc impact a11y nul, mais visuel bruyant si un futur statut FR introduit des caractères spéciaux. Slugifier proprement si nécessaire : `.normalize("NFKD").replace(/[̀-ͯ]/g, "").replace(/[^a-z0-9]+/g, "_")`.
- ~~**Nom long carte méthodo (`<h3>` `text-display-sm` 28px) — risque overflow à ~320px**~~ — **RÉSOLU (Story 4.2 AC#3/#5, 2026-05-13)** : `wrap-break-word` (Tailwind v4 canonical pour `overflow-wrap: break-word`) ajouté au `<h3>` de `MethodologyCard.tsx`. Smoke 320×568 + 360×640 + 375×667 sur `/en` ET `/fr` passé sans débordement — vérifié via Chrome DevTools MCP (emulation `mobile,touch,deviceScaleFactor=2`).

## Deferred from: code review of story-3.1 (2026-05-13)

- **Pas de listener `matchMedia('change')` sur `prefers-reduced-motion` dans `useScrollFadeIn`** — [src/hooks/useScrollFadeIn.ts:14-22](src/hooks/useScrollFadeIn.ts#L14-L22) lit la media query une seule fois au montage du composant. Si l'utilisateur active/désactive PRM en cours de session (OS-level), les wrappers `FadeIn` déjà montés ne se ré-évaluent pas — asymétrie vs `Clients.tsx` dont le `@media (prefers-reduced-motion: reduce)` CSS est ré-évalué dynamiquement par le navigateur. Impact faible (changement de préférence en cours de session est rare). Largement résolu si le patch `motion-reduce:transition-none` est appliqué (transition CSS suit la media query en temps réel). Sinon, ajouter `mql.addEventListener('change', handler)` dans l'effect.
- **Contrat `items: readonly { name: string }[]` n'enforce ni non-vide ni longueur minimale pour le marquee** — [src/components/Clients.tsx:9](src/components/Clients.tsx#L9). Avec `items === []`, `tripled` est vide → animation tourne sur un container 0px (CPU négligeable). Avec `items.length === 1`, `translateX(-33.333%)` ne correspond plus à un cycle visuellement seamless. Aujourd'hui le dictionnaire fournit 4 items (`en.ts:76-81`, `fr.ts`) — pas de risque réel. À durcir si la liste devient dynamique : type tuple `readonly [{ name: string }, { name: string }, ...]` ou guard `if (items.length < 2) return null;`.
- **`width: max-content` peut être plus étroit que le viewport sur tablette si `items.name` sont courts** — [src/components/Clients.tsx:31](src/components/Clients.tsx#L31). Sur iPad 1024–1180 px, 12 wordmarks longs (`text-marquee` 44px) + séparateurs ≈ 2300–3500 px → la marge est confortable avec les 4 maisons actuelles ("Louis Vuitton", "Dior", "Messika", "Tiffany & Co."). Si un futur item est court (ex. "MM"), le triplicat peut tomber sous la largeur du viewport, révélant du `bg-bg-alt` à droite pendant le cycle. Mitigation : `min-width: 200vw` sur l'inner container, ou logique de duplication ×N adaptative basée sur la longueur totale mesurée.

## Deferred from: code review of story-3.2 (2026-05-13)

- **`mix-blend-mode: difference` peut réduire le contraste du dot par-dessus la `Nav` `backdrop-filter`** — [src/app/globals.css:192](src/app/globals.css#L192) (`mix-blend-mode: difference` sur `.cursor-dot`) + [src/components/Nav.tsx:58](src/components/Nav.tsx#L58) (`backdrop-blur-md`). Chaque élément crée son propre stacking context : le dot ne mixe qu'avec les pixels composités du calque ambient, pas avec ce qui est sous la nav. Selon la couleur du résultat composité, le dot peut s'approcher du `#d4a574` (quasi-invisible) ou rester lisible. Audit visuel ciblé recommandé en **Story 9.1** (polish pré-lancement) ou Story 4.2.
- **`html.cursor-none` ne couvre pas `input/textarea/select/[contenteditable]/label/summary/[role="button"]`** — [src/app/globals.css:174-178](src/app/globals.css#L174-L178) — les UA stylesheets imposent `cursor: text` (inputs) ou `cursor: pointer` (`[role="button"]`) avec une spécificité ≥ `body`. Sur ces éléments, le curseur système réapparaît en double du custom. Aucun formulaire dans le site MVP — élargir la liste (ou passer à `html.cursor-none *`) quand **Story 4.x / 9.1** introduira inputs ou contact form.
- ~~**`requestAnimationFrame` tourne en continu même quand la souris est immobile**~~ — **RÉSOLU (Story 4.2 AC#4, 2026-05-13)** : idle gating ajouté dans la boucle RAF de `CustomCursor.tsx`. Quand `Math.abs(x - rx) < 0.1 && Math.abs(y - ry) < 0.1` (seuil sub-pixel), `idle = true` et la boucle n'est plus ré-enchaînée ; reprise au prochain `mousemove` via test `if (idle)` dans `onMove`. Cleanup inchangé (`cancelAnimationFrame(raf)` reste un no-op silencieux si la boucle est déjà stoppée). Le `dot` (positionné instantanément dans `onMove`, sans lerp ni RAF) n'est pas concerné.
- ~~**Pas de retour visuel du curseur custom sur navigation clavier (focus)**~~ — **RÉSOLU (Story 4.1 AC#7, 2026-05-13)** : décision documentée dans `CustomCursor.tsx` — l'anneau `focus-visible` accent (outline 2px) reste l'indicateur primaire de focus clavier ; doubler avec un déplacement du ring custom serait redondant et coûteux. Le curseur est délibérément souris-only (`(hover: hover) and (pointer: fine)`).
- **Curseur custom reste figé en bord de viewport quand la souris quitte la fenêtre** — pas de listener `mouseleave` sur `document`/`window` dans [src/components/CustomCursor.tsx](src/components/CustomCursor.tsx). Dot/ring stationnent au bord puis snappent au retour de la souris par un autre bord. Polish différé — non bloquant.
- **`z-index: 9999/9998` valeurs magiques sans token partagé** — [src/app/globals.css:188, 204](src/app/globals.css#L188) — aucune collision aujourd'hui (pas de modal/toast). À documenter en convention (token `--z-cursor`) quand des overlays (modal, dialog Radix, toast Story 5.x) seront introduits.
- **Iframes : `mousemove` ne propage pas au document parent** — [src/components/CustomCursor.tsx:95](src/components/CustomCursor.tsx#L95) — aucun iframe aujourd'hui. Une future intégration vidéo/embed (Vimeo, Spotify, YouTube) figerait le curseur custom au bord de l'iframe. À traiter au moment de l'intégration (embed video → cacher curseur ou afficher système au survol de l'iframe).
- **OS-rendered dropdowns / `<select>` natifs / `<input type="file">` ne sont pas couverts** — [src/components/CustomCursor.tsx](src/components/CustomCursor.tsx) — pas de `<select>` natifs ni d'upload aujourd'hui. Quand **Story 4.x / 9.1** introduira un formulaire, écouter `pointerlockchange`/`fullscreenchange` ou démonter le curseur custom au focus d'un control natif.

## Deferred from: code review of story-4.1 (2026-05-13)

- **AC#8 — audit axe + Lighthouse runtime délégué à Mike** — l'audit navigateur (extension Chrome axe DevTools + Lighthouse Accessibility) n'est pas scriptable depuis l'agent dev (profil Chrome MCP verrouillé). Story marquée `review` (et non `done`) précisément pour cette validation finale par Mike. Action : reporter dans les Completion Notes les 4 scores Lighthouse (a11y /en mobile + desktop, /fr mobile + desktop) et le compte de violations axe sur les 2 locales. À traiter avant passage à `done`.
- **`querySelector("a, button")` du focus initial Nav ne filtre pas `disabled`/`tabindex=-1`/`aria-disabled`** — [src/components/Nav.tsx:108](src/components/Nav.tsx#L108). Sélecteur sous-spécifié pour un pattern APG disclosure strict. Aucun cas aujourd'hui (tous les enfants du panneau sont focusables), mais régression future possible si un `<button disabled>` ou autre type non interactif est introduit en première position. Étendre le sélecteur (`a:not([aria-disabled="true"]), button:not([disabled]):not([aria-disabled="true"])`) lors d'une future itération a11y.
- **Garde `satisfies Dictionary` unidirectionnelle (EN non validé)** — [src/i18n/dictionaries/en.ts](src/i18n/dictionaries/en.ts) — `en.ts` n'utilise pas `as const` (convention explicite documentée ligne 12), donc `typeof en` est la source de vérité dérivée pour `Dictionary`. Si un dev retire accidentellement une clé d'`en.ts`, `fr.ts` continuera de passer typecheck. Confer la dette plus large « complétude FR/EN aveugle aux tableaux » (review 1.3). Renforcer impliquerait de revoir la convention en.ts.
- **`scroll-mt-nav-height` non testé par typecheck** — [src/app/globals.css](src/app/globals.css) + [src/components/GridSection.tsx:46](src/components/GridSection.tsx#L46). Tailwind v4 dérive l'utilitaire automatiquement de `--spacing-nav-height`, mais sans plugin lint Tailwind, un renommage du token (`--spacing-nav-height` → autre) devient une régression silencieuse (la classe `scroll-mt-nav-height` n'est plus générée, `scroll-margin-top` retombe à 0). Ajouter un plugin de validation Tailwind à terme.
- **Conflit Escape futur avec LanguageSwitcher si converti en dropdown** — [src/components/Nav.tsx:111-115](src/components/Nav.tsx#L111-L115) — le listener `keydown` Escape est attaché au `document` (non scopé au panneau). Pas de conflit aujourd'hui (LanguageSwitcher = 2 boutons simples), mais si une future itération en fait un dropdown Radix Combobox/Select consommant aussi Escape, double-action involontaire (ferme le menu + ferme le dropdown). À ré-évaluer au moment de toute évolution du LanguageSwitcher.
- **`getDictionary(locale)` appelé 2× par requête (layout + page)** — [src/app/[locale]/layout.tsx:87](src/app/[locale]/layout.tsx#L87) + [src/app/[locale]/page.tsx:34](src/app/[locale]/page.tsx#L34) — pas de mémoïsation explicite ; Node module cache rend le 2e import quasi-gratuit. Perf négligeable en SSG (build-time, 2 pages × 2 locales = 4 invocations). À mémoïser (via `React.cache` ou singleton module-level) si bascule en SSR dynamique futur.
- **Sélecteur du focus initial omet `input/select/textarea/[tabindex]/[contenteditable]`** — [src/components/Nav.tsx:108](src/components/Nav.tsx#L108) — couplage implicite : ne pas mettre un autre type de focusable AVANT le 1er `<a>`/`<button>` du panneau sans mettre à jour le sélecteur. Aucun cas aujourd'hui.

## Deferred from: code review of story-2.4 (2026-05-13)

- **PII (URL LinkedIn + numéro téléphone) dupliqués dans `en.ts` et `fr.ts`** — `en.ts:334–335` / `fr.ts` — `secondaryLinks[0].value` (URL LinkedIn) et `[1].value` (téléphone) sont identiques dans les deux locales (non localisées) mais stockés en double. Tout changement d'une des deux valeurs nécessite deux éditions séparées pouvant diverger silencieusement — la garde `satisfies Dictionary` est aveugle au contenu des tableaux. Consolider dans `meta.linkedin` / `meta.phone` lors de **Story 9.1** (fix LinkedIn 404 déjà prévu).
- **`tel:` normalisation trop étroite dans `Contact`** — [src/components/Contact.tsx:100](src/components/Contact.tsx#L100) — `.replace(/\s+/g, "")` ne strip que les espaces. Safe avec `+972 58 422 0567`; un format avec parenthèses (`+972 (58) 422-0567`) ou tirets produirait un `tel:` URI techniquement valide mais rejeté par certains dialers. Étendre le regex si le format du numéro change.
- ~~**Entrées non-cliquables (Location, Languages) : affordance identique aux `<a>` cliquables**~~ — **RÉSOLU (Story 4.1 AC#6, 2026-05-13)** : branche else de `Contact.tsx` simplifiée — les `<div>` non-cliquables rendent en `<div className="flex items-center px-5 py-2">` (sans encadré, sans `min-h-11`, mais `px-5` préservé pour aligner verticalement le label mono avec celui des `<a>` voisins — Story 4.1 review patch P12). Distinction visuelle évidente vs affordance d'interactivité des `<a>` LinkedIn/Phone bordés.
- ~~**Smoke browser à ~320px non exécuté pour la section Contact**~~ — **RÉSOLU (Story 4.2 AC#3, 2026-05-13)** : smoke 320×568 via Chrome DevTools MCP a effectivement révélé un débordement de **29 px** dans la section `#contact` — coupable identifié : l'URL LinkedIn longue de 47 chars sans espace (`https://www.linkedin.com/in/michaelmann-339545149`) imposait une largeur intrinsèque > viewport. Fix : `min-w-0` sur le `<div>` parent (autorise le flex item à se contracter sous sa largeur min-content) + `wrap-anywhere` (CSS `overflow-wrap: anywhere`) sur le `<div>` contenant `link.value` (casse l'URL au besoin). Re-smoke après fix : `docOverflow: false` sur 320 / 360 / 375 / 1440 / 1920 × `/en` + `/fr`. Bouton email (`michael.mann55@gmail.com`, 27 chars) et bouton CV restent OK à 320px (vérifié visuellement par screenshot).

## Deferred from: code review of story-4.2 (2026-05-13)

- **Bundle First Load JS ≈ 189 KB gzip > budget AC 150 KB (Story 4.2 AC#2)** — [_bmad-output/implementation-artifacts/4-2-budget-de-performance-core-web-vitals.md](4-2-budget-de-performance-core-web-vitals.md) — Mesure rapportée par Amelia (Tâche 2, `npx next experimental-analyze --output` + analyse des chunks référencés par `/en.html`) : ~632 KB raw / ~189 KB gzip sur 9 chunks. **Dépasse le budget AC strict de ~39 KB gzip.** Cause attribuée au plancher Next 16.2.6 + React 19 (plus élevé que le plancher Next 14 + React 18 pour lequel le budget 150 KB gzip a été défini). **Décision Mike (code review 2026-05-13) : accepté conditionnellement à `Lighthouse Performance Mobile ≥ 95` sur `/en` + `/fr`** — si la passe Lighthouse officielle (Tâche 7) confirme ce seuil, la dette est assumée et le budget AC est de fait muté en « Performance ≥ 95 ». **Si Lighthouse < 95**, la cascade de mitigation AC#2 se réouvre : (1) frontière `'use client'` resserrée (probable cible : `Nav.tsx` scroll-spy + `LanguageSwitcher.tsx`) ; (2) `next/dynamic` plus agressif sur les composants Client non-LCP ; (3) remplacement d'une dépendance lourde par une implémentation plus petite/native. Suivi long-terme via **Story 7.2** (CI Lighthouse durci) qui ajoutera un gate sur le bundle.
- **Guard `Number.isFinite(x) && Number.isFinite(y)` dans `loop()` de `CustomCursor.tsx`** — [src/components/CustomCursor.tsx:99](src/components/CustomCursor.tsx#L99) — Cas marginal pour événements synthétiques (tests, polyfills, extensions). Si `x` ou `y` devient `NaN`/`Infinity`, `Math.abs(NaN) < 0.1` est `false` → la boucle ne s'idle JAMAIS (régression silencieuse de la dette 3.2 résolue par AC#4). Aucun cas réel observé. À durcir si un cas QA automatisé l'expose.
- **URL LinkedIn — préférer `<wbr>` aux frontières logiques plutôt que `wrap-anywhere`** — [src/components/Contact.tsx:117-121](src/components/Contact.tsx#L117-L121) — `wrap-anywhere` peut casser à un caractère arbitraire (ex. « michaelmann-|33954... ») et bruiter le copier-coller utilisateur avec des sauts de ligne selon le navigateur. Pratique idiomatique pour les URLs longues : `<wbr>` aux frontières logiques (`/`, `-`). Fonctionnel aujourd'hui ; UX-polish à reconsidérer en **Story 9.1** (audit pré-lancement).
- **`wrap-anywhere` over-broad sur les entrées non-LinkedIn (Languages/Location)** — [src/components/Contact.tsx:117-121](src/components/Contact.tsx#L117-L121) — La factorisation `labelBlock` applique `wrap-anywhere` aux 4 entrées de `secondaryLinks` alors que seul l'URL LinkedIn en avait besoin. Pour `"Ashdod, Israël"` / `"Français · Hébreu · Anglais"`, autorise une coupure cosmétiquement laide (« Fran|çais ») sous viewport < 240px utile. Hors viewport standard, non visible.
- **Focus-ring `outline-offset-2` sur lien LinkedIn à 320px peut dépasser viewport** — [src/components/Contact.tsx:128-146](src/components/Contact.tsx#L128-L146) — Touche WCAG 2.1 SC 2.4.11 (focus visible occluded). Quand l'URL wrap sur 3 lignes à 320px, l'`outline-2 outline-offset-2` ajoute 4 px externe ; en bord droit du viewport, peut être légèrement coupé. `--spacing-section-x-mobile` = 20 px couvre partiellement l'offset. À vérifier visuellement en smoke a11y de **Story 9.1**.
- ~~**Politique AGENTS.md : `priority` réservé au LCP — silencieuse sur multi-image**~~ — **RÉSOLU (Story 4.3 AC#12, 2026-05-13)** : politique multi-image ajoutée à `AGENTS.md` section Images — un seul asset `priority` (LCP candidate), les autres `lazy`.
- **`min-w-0` défensif sur `<article>` parent `MethodologyCard`** — [src/components/MethodologyCard.tsx:21](src/components/MethodologyCard.tsx#L21) — Inutile aujourd'hui car la grille parente (`Projects.tsx`, `grid-cols-1`) résout déjà la contraction (tracks `minmax(0, 1fr)` par défaut Tailwind v4). Filet défensif si une future refonte place `MethodologyCard` dans un `flex` parent sans `min-w-0`.
- **`min-w-0` redondant sur la branche non-cliquable de `Contact.tsx`** — [src/components/Contact.tsx:106-123](src/components/Contact.tsx#L106-L123) — La factorisation `labelBlock` applique `min-w-0` aux 2 branches (link `<a>` et non-link `<div>`). Sur la branche non-cliquable (`<div className="flex items-center px-5 py-2">`), `labelBlock` est l'unique enfant flex → `min-w-0` n'a rien à contraindre. Inoffensif, juste cosmétique.

## Deferred from: code review of story-4.3 (2026-05-13)

- **OG image — `v2026.1` hardcoded version label** — [src/app/opengraph-image.tsx:62](src/app/opengraph-image.tsx#L62) — La chaîne `· michael-mann · portfolio · v2026.1 ·` contient un numéro de version arbitraire non lié à `package.json` ni à un tag git. Vieillit mal — une OG image générée le 2027 affichera toujours « v2026.1 ». Solutions : retirer la version, la dériver de `process.env.npm_package_version`, ou la lier à un build tag CI. Polish cosmétique — à reconsidérer en Story 9.1 (audit pré-lancement).
- **Lighthouse — `numberOfRuns=1` flaky** — [.github/workflows/lighthouse.yml:43](.github/workflows/lighthouse.yml#L43) — Lighthouse est notoirement bruité (±5-10 points sur Performance). Un seul run produira des assertions instables. La médiane de 3 runs est le minimum recommandé. Per-spec MVP advisory, à durcir en **Story 7.2** (CI Lighthouse durci).
- **Lighthouse — `npm run start &` sans cleanup ni log capture** — [.github/workflows/lighthouse.yml:34](.github/workflows/lighthouse.yml#L34) — Le serveur est lancé en background mais aucun handle, aucun cleanup, aucune capture de stdout/stderr. Si `next start` échoue (port pris, build absent), `wait-on` timeout après 60s puis l'étape suivante échoue silencieusement avec `continue-on-error: true`. Hygiène CI à durcir en **Story 7.2** : `npm run start > server.log 2>&1 &` + upload du log en cas d'échec.
- **AC#11 — préflight Lighthouse local non reporté dans Completion Notes** — Story 4.3 Tâche 11 demandait « **Tester en local** (avant de pousser à GitHub) … Confirmer que Lighthouse tourne sans crash ». Les Completion Notes AC#11 mentionnent « Tests sur runner GitHub réel délégués à Mike (premier push de la branche) » — la validation locale n'a pas été exécutée. Si le job échoue sur runner GitHub (chrome deps, timing), une nouvelle entrée dans cette section sera à créer pour différer le durcissement en Story 7.2.
- **AC#14 — First Load JS bundle non re-mesuré post-build** — Story 4.3 AC#14 demandait « reporter First Load JS dans Completion Notes ». Le report indique « non re-mesuré spécifiquement … La valeur Story 4.2 (~189 KB gzip) reste la référence ». Justification (aucun nouveau Client component) plausible mais ne couvre pas la lettre de l'AC. Re-mesurer ponctuellement quand utile (Story 9.1 pré-lancement).
- **AC#1 — sortie `next build` Route (app) simplifiée dans Completion Notes** — Story 4.3 AC#1 demandait « Reporter la sortie « Route (app) » dans les Completion Notes (cohérent avec template Tâche 1 Story 4.2) ». Le report liste les 8 routes mais sans les colonnes `Size` / `First Load JS` du tableau Next 16 standard. Polish documentaire.
- **`sitemap.lastModified = new Date()` change à chaque build** — [src/app/sitemap.ts:15](src/app/sitemap.ts#L15) — Le timestamp est figé au build (cf. doc Next 16) mais change à chaque re-deploy, même sans modification de contenu. Google verra une « dernière modif » permanente et peut recrawler inutilement. Mieux : dériver de la date du dernier commit Git affectant `src/app/[locale]/page.tsx`, ou utiliser une constante. À reconsidérer si Search Console signale un over-crawl.
- **`manifest.start_url: "/en"` hardcoded sans couplage à `defaultLocale`** — [src/app/manifest.ts:13](src/app/manifest.ts#L13) — Per-spec actuellement, mais si `defaultLocale` change un jour pour `fr` dans [src/i18n/config.ts](src/i18n/config.ts), le manifest restera figé sur `/en`. Coupler à `defaultLocale` ou laisser `start_url: "/"` (le proxy redirigera). Future-proof.
- **OG image en anglais uniquement malgré `og:locale=fr_FR` sur `/fr`** — [src/app/opengraph-image.tsx:62-110](src/app/opengraph-image.tsx#L62-L110) — Le contenu visuel de l'OG image (`Senior Frontend Developer`, `location → Ashdod, Israel`, `experience → 5+ years`, `focus → React · TypeScript · Supabase`, `$ open mann.dev →`) est 100% anglais. Quand un partage `/fr` récupère l'image, contenu visuel incohérent avec `og:title`/`og:description` français. Décision MVP per-spec (AC#4 lignes 87-93). À ré-évaluer en Story 9.1 si Mike juge l'incohérence visible sur les partages FR (créer une variante `opengraph-image.tsx` sous `[locale]/` doublerait fichier + pipeline assets — d'où le différé).
